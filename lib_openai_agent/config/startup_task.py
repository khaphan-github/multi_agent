"""
Startup tasks for the application.
This module defines tasks that are executed at application startup.
"""

# Import existing startup tasks

# Import the admission AI chatbot startup task

# Add the admission AI startup task to the list of startup tasks
APP_START_UP_TASKS = [
]
